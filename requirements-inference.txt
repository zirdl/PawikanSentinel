# Core web dependencies (shared with main app)
fastapi==0.103.2
uvicorn==0.23.2
python-dotenv==1.0.0
jinja2==3.1.2
python-multipart==0.0.20
websockets==11.0.3

# YOLO11 inference (replaces Roboflow inference-sdk)
ultralytics>=8.3.0,<8.4.0
huggingface_hub>=0.20.0
# Optional: for ONNX export (run on dev machine, not needed on Pi if using .pt directly)
# onnx>=1.15.0
# onnxruntime>=1.17.0

# SMS notifications
requests>=2.31.0

# System monitoring (Pi optimization)
psutil>=5.9.0

# Computer vision
opencv-python-headless>=4.8.0
