[project]
name = "pawikansentinel"
version = "0.1.0"
description = "AI-powered sea-turtle detection on Raspberry Pi 4B with SMS alerts"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "opencv-python-headless",
    "numpy",
    "twilio",
]
